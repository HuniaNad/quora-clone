<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Navbar</a>
    <div class="d-flex flex-row">
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <li class="nav-item"> 
            <%= link_to edit_user_registration_path do %>
              <% if current_user&.image.attached? %>
                <%= image_tag(current_user.image, style: 'height: 50px; width: 50px; display: inline-block; border-radius: 50%;
                object-fit: cover;') %>
              <% else %>
                <span class="material-icons md-48">
                account_circle
                </span>
              <% end %>
            <% end %> 
          
          </li>
          <li class="nav-item"> <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-small btn-primary mx-2" %></li>
        <% else %>
          <li class="nav-item"> <%= link_to "Sign Up", new_user_registration_path, class: "nav-link"  %></li>
          <li class="nav-item"> <%= link_to "Sign In", new_user_session_path, class: "nav-link"  %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>