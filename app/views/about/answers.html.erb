<%= render partial: 'tab_menu', locals: { page: 'answers' }  %>

<div class="tab-content" id="nav-tabContent">
  <%= render partial: 'answers', collection: @answers, as: :answer %>
  <%= paginate @answers %>
</div>

  