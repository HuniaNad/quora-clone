

<div class="container mt-4">
    <div class="row">
    <div class="col-md-8 offset-md-2">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center mb-3">
                    <% if associated_questions&.user&.image&.attached? %>
                    <%= image_tag(associated_questions.user.image, style: 'height: 30px; width: 30px; display: inline-block; border-radius: 50%;') %>
                    <% else %>
                    <span class="material-icons md-24 rounded-circle">
                        account_circle
                    </span>
                    <% end %>
                    <h5>
                        <%= link_to associated_questions.user.name, profile_path(associated_questions.user_id) %>
                    </h5>
                </div>

                <%= link_to 'Open', associated_questions, class: 'btn btn-warning' %>
            </div>
            
            <div class="card-body">
                <h5 class="card-title">
                    <%= associated_questions.title.titlecase %>
                </h5>
                <p class="card-text">
                    <%= truncate(associated_questions.body.capitalize, length: 140) %>
                </p>
            </div>

            <div class="card-footer text-muted d-flex justify-content-between align-items-center">
                Posted on <%= associated_questions.created_at.strftime('%B %e, %Y %l:%M %p') %>
            </div>
        </div>
    </div>
    </div>
</div>  